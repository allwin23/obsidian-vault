---
tags:
  - coding-questions
  - dsa
status:
---
0## 📋 Info

| Field               | Value                                                                         |
| ------------------- | ----------------------------------------------------------------------------- |
| **Question Number** | 1                                                                             |
| **Platform**        |  Chat                                                                         |
| **Difficulty**      | 🟢 Easy / 🟡 Medium / 🔴 Hard                                                 |
| **Topic**           | [[Recursion]]                                                                 |
| **Problem Link**    | [Link](https://takeuforward.org/data-structure/subset-sum-sum-of-all-subsets) |
| **Date Solved**     | 2025-12-19                                                                    |
| Intuition Log       |                                                                               |


---

## ❓ Question:

> You need create a sum result of every possible subset 



## 

---

## 💻 Solution :


```cpp
#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    // Helper function to generate subset sums recursively
    void findSums(int index, int currentSum, vector<int>& arr, vector<int>& sums) {
        // Base case: if we have considered all elements
        if (index == arr.size()) {
            sums.push_back(currentSum);
            return;
        }

        // Include current element
        findSums(index + 1, currentSum + arr[index], arr, sums);

        // Exclude current element
        findSums(index + 1, currentSum, arr, sums);
    }

    // Main function
    vector<int> subsetSums(vector<int>& arr) {
        vector<int> sums;
        findSums(0, 0, arr, sums);
        sort(sums.begin(), sums.end()); // Sort in increasing order
        return sums;
    }
};

// Driver code
int main() {
    Solution sol;
    vector<int> arr = {5, 2, 1};
    vector<int> result = sol.subsetSums(arr);

    for (int sum : result) {
        cout << sum << " ";
    }
    cout << endl;

    return 0;
}

```



---


## ⏱️ Complexity :

| Type      | Complexity |
| --------- | ---------- |
| **Time**  | O(2^N)     |
| **Space** | O()        |

---

## 💡 Key Insight :

there is something called leap of faith and decision tress concepts in the recursion side where we need take a decision for all the case of scenarios to make up the To make up the subset of everything I want you to take bread crumb mark it and explore all the option and remove it at the end this is how we should be able to change all the case of request 

---

## 🔗 Related Questions

|Question|Topic|Difficulty|
|---|---|---|
|[[]]|||
|[[]]|||
