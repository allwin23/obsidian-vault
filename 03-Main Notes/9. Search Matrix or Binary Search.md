---
tags:
  - coding-questions
  - dsa
status:
---
## 📋 Info

| Field               | Value                                                                            |
| ------------------- | -------------------------------------------------------------------------------- |
| **Question Number** | 9                                                                                |
| **Platform**        | LeetCode                                                                         |
| **Difficulty**      | 🟢 Easy /                                                                        |
| **Topic**           | [[Arrays]]                                                                       |
| **Problem Link**    | [Link](https://leetcode.com/problems/search-a-2d-matrix/submissions/1800570080/) |
| **Date Solved**     | {{date}}                                                                         |
| Intuition Log       |                                                                                  |


---

## ❓ Question:

> You are given an `m x n` integer matrix `matrix` with the following two properties:

- Each row is sorted in non-decreasing order.
- The first integer of each row is greater than the last integer of the previous row.

Given an integer `target`, return `true` _if_ `target` _is in_ `matrix` _or_ `false` _otherwise_.

You must write a solution in `O(log(m * n))` time complexity.



## **Constraints:**

---

## 💻 Solution :


```cpp

class Solution {
public:
    bool searchMatrix(vector<vector<int>>& matrix, int target) {
        int m = matrix.size();
        int n= matrix[0].size();
        int i =0;
        int j=m*n-1;
        while (i<=j){
            int mid = i + (j-i) /2;
            int row = mid/n;
            int col=mid%n;
            if(target == matrix[row][col]){
                 return true ;

            }else if(target > matrix[row][col]){
                   i = mid +1;

            }else{
                  j= mid-1;
            }


        }
        return false;
        
    }
};
```


---


## ⏱️ Complexity :

| Type      | Complexity |
| --------- | ---------- |
| **Time**  | O(n/2)     |
| **Space** | O(1)       |

---

## 💡 Key Insight :
here it is a interesting binary search algorithm will be used here 
its very simple need to convert the overall value into matrix form 
simple hack would be div for row and mod for col be careful always use mid value for i + j-i /2 the value could be starting from after i so i +1 or j -1 update the i and j with the mid 

---

## 🔗 Related Questions

|Question|Topic|Difficulty|
|---|---|---|
|[[]]|||
|[[]]|||
