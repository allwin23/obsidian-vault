---
tags:
  - coding-questions
  - dsa
status:
---
## 📋 Info

| Field               | Value                                                            |
| ------------------- | ---------------------------------------------------------------- |
| **Question Number** | 1                                                                |
| **Platform**        | Leet Code                                                        |
| **Difficulty**      | 🟡 Medium                                                        |
| **Topic**           | [[Arrays]]                                                       |
| **Problem Link**    | [[https://leetcode.com/problems/set-matrix-zeroes/description/]] |
| **Date Solved**     | 2025-12-16                                                       |
| Intuition Log       |                                                                  |


---

## ❓ Question:

Given an `m x n` integer matrix `matrix`, if an element is `0`, set its entire row and column to `0`'s.

You must do it [in place](https://en.wikipedia.org/wiki/In-place_algorithm).

**Example 1:**

![](https://assets.leetcode.com/uploads/2020/08/17/mat1.jpg)

**Input:** matrix = [[1,1,1],[1,0,1],[1,1,1]]
**Output:** [[1,0,1],[0,0,0],[1,0,1]]

**Example 2:**

![](https://assets.leetcode.com/uploads/2020/08/17/mat2.jpg)

**Input:** matrix = [[0,1,2,0],[3,4,5,2],[1,3,1,5]]
**Output:** [[0,0,0,0],[0,4,5,0],[0,3,1,0]]

**Constraints:**

- `m == matrix.length`
- `n == matrix[0].length`
- `1 <= m, n <= 200`
- `-231 <= matrix[i][j] <= 231 - 1`



## **Constraints:**

---

## 💻 Solution :



```cpp
class Solution {
public:
    void setZeroes(vector<vector<int>>& matrix) {
        int row = matrix.size();
        int col = matrix[0].size();
        bool firstRowZero = false;
        bool firstColZero = false;

        // Step 1: check first row
        for (int j = 0; j < col; j++) {
            if (matrix[0][j] == 0) {
                firstRowZero = true;
                break;
            }
        }

        // Step 2: check first column
        for (int i = 0; i < row; i++) {
            if (matrix[i][0] == 0) {
                firstColZero = true;
                break;
            }
        }

        // Step 3: mark rows/cols using first row & col
        for (int i = 1; i < row; i++) {
            for (int j = 1; j < col; j++) {
                if (matrix[i][j] == 0) {
                    matrix[i][0] = 0;
                    matrix[0][j] = 0;
                }
            }
        }

        // Step 4: set cells based on markers
        for (int i = 1; i < row; i++) {
            for (int j = 1; j < col; j++) {
                if (matrix[i][0] == 0 || matrix[0][j] == 0) {
                    matrix[i][j] = 0;
                }
            }
        }

        // Step 5: fix first row
        if (firstRowZero) {
            for (int j = 0; j < col; j++) {
                matrix[0][j] = 0;
            }
        }

        // Step 6: fix first column
        if (firstColZero) {
            for (int i = 0; i < row; i++) {
                matrix[i][0] = 0;
            }
        }
    }
};
```
---

## ⏱️ Complexity :

| Type      | Complexity |
| --------- | ---------- |
| **Time**  | O(n)       |
| **Space** | O(1)       |

---

## 💡 Key Insight 

the very important thing I remember here to solve is I struggled to solve the in place Problem 
here they basically use the first row and column as markers and traverse through an array and fix where the problem is 
the issue is you ignore the first row 
for that we have a Bool check for the column and make it check for all 



---

## 🔗 Related Questions

| Question | Topic | Difficulty |
| -------- | ----- | ---------- |
| [[]]     |       |            |
| [[]]     |       |            |
