<%*
// No prompt — always builds the CURRENT month. Run it on/after the 1st.
const input = tp.date.now("YYYY-MM");
const [y, m] = input.split("-").map(Number);
const daysInMonth = new Date(y, m, 0).getDate();
const monthName = new Date(y, m - 1, 1).toLocaleString('default', { month: 'long' });

let front = "---\n";
front += `month: ${input}\n`;
for (let d = 1; d <= daysInMonth; d++) {
  const dd = String(d).padStart(2, '0');
  front += `planned_${dd}: \n`;
  front += `worked_${dd}: \n`;
}
front += "---\n\n";

let body = `# Time Efficiency — ${monthName} ${y}\n\n`;
body += "**Required Efficiency** = Worked Hrs ÷ 12 (fixed daily standard)  \n";
body += "**Actual Efficiency** = Worked Hrs ÷ Planned Hrs (against what you planned)\n\n";

body += "```dataviewjs\n";
body += "const fm = dv.current();\n";
body += "const file = app.vault.getAbstractFileByPath(fm.file.path);\n";
body += "const month = fm.file.name;\n";
body += "const [y, m] = month.split('-').map(Number);\n";
body += "const daysInMonth = new Date(y, m, 0).getDate();\n";
body += "\n";
body += "const container = dv.el('div', '', { cls: 'eff-time-wrap' });\n";
body += "const table = document.createElement('table');\n";
body += "table.className = 'eff-table';\n";
body += "\n";
body += "const headerRow = document.createElement('tr');\n";
body += "['Date', 'Planned Hrs', 'Worked Hrs', 'Required Eff.', 'Actual Eff.'].forEach(h => {\n";
body += "  const th = document.createElement('th');\n";
body += "  th.textContent = h;\n";
body += "  headerRow.appendChild(th);\n";
body += "});\n";
body += "table.appendChild(headerRow);\n";
body += "\n";
body += "for (let d = 1; d <= daysInMonth; d++) {\n";
body += "  const dd = String(d).padStart(2, '0');\n";
body += "  const wd = new Date(y, m - 1, d).toLocaleDateString('default', { weekday: 'short' });\n";
body += "  const row = document.createElement('tr');\n";
body += "\n";
body += "  const dateCell = document.createElement('td');\n";
body += "  dateCell.textContent = `${d} ${wd}`;\n";
body += "  row.appendChild(dateCell);\n";
body += "\n";
body += "  const plannedCell = document.createElement('td');\n";
body += "  const plannedInput = document.createElement('input');\n";
body += "  plannedInput.type = 'number';\n";
body += "  plannedInput.placeholder = 'hrs';\n";
body += "  plannedInput.value = fm['planned_' + dd] ?? '';\n";
body += "  plannedCell.appendChild(plannedInput);\n";
body += "  row.appendChild(plannedCell);\n";
body += "\n";
body += "  const workedCell = document.createElement('td');\n";
body += "  const workedInput = document.createElement('input');\n";
body += "  workedInput.type = 'number';\n";
body += "  workedInput.placeholder = 'hrs';\n";
body += "  workedInput.value = fm['worked_' + dd] ?? '';\n";
body += "  workedCell.appendChild(workedInput);\n";
body += "  row.appendChild(workedCell);\n";
body += "\n";
body += "  const reqCell = document.createElement('td');\n";
body += "  reqCell.className = 'eff-value';\n";
body += "  row.appendChild(reqCell);\n";
body += "\n";
body += "  const actCell = document.createElement('td');\n";
body += "  actCell.className = 'eff-value';\n";
body += "  row.appendChild(actCell);\n";
body += "\n";
body += "  const recompute = () => {\n";
body += "    const w = parseFloat(workedInput.value);\n";
body += "    const p = parseFloat(plannedInput.value);\n";
body += "    reqCell.textContent = isNaN(w) ? '—' : ((w / 12) * 100).toFixed(1) + '%';\n";
body += "    actCell.textContent = (isNaN(w) || isNaN(p) || p === 0) ? '—' : ((w / p) * 100).toFixed(1) + '%';\n";
body += "  };\n";
body += "  recompute();\n";
body += "\n";
body += "  const persist = async () => {\n";
body += "    await app.fileManager.processFrontMatter(file, (frontmatter) => {\n";
body += "      frontmatter['planned_' + dd] = plannedInput.value === '' ? null : parseFloat(plannedInput.value);\n";
body += "      frontmatter['worked_' + dd] = workedInput.value === '' ? null : parseFloat(workedInput.value);\n";
body += "    });\n";
body += "  };\n";
body += "\n";
body += "  plannedInput.addEventListener('input', recompute);\n";
body += "  workedInput.addEventListener('input', recompute);\n";
body += "  plannedInput.addEventListener('change', persist);\n";
body += "  workedInput.addEventListener('change', persist);\n";
body += "\n";
body += "  table.appendChild(row);\n";
body += "}\n";
body += "\n";
body += "container.appendChild(table);\n";
body += "```\n";

body += "\n---\n\n## Month Summary\n\n";
body += "```dataviewjs\n";
body += "const p = dv.current();\n";
body += "const rows = [];\n";
body += "let reqTotal = 0, reqCount = 0, actTotal = 0, actCount = 0;\n";
body += `for (let d = 1; d <= ${daysInMonth}; d++) {\n`;
body += "  const dd = String(d).padStart(2, '0');\n";
body += "  const planned = p['planned_' + dd];\n";
body += "  const worked = p['worked_' + dd];\n";
body += "  if (worked === undefined || worked === null || worked === '') continue;\n";
body += "  const req = (worked / 12) * 100;\n";
body += "  reqTotal += req; reqCount++;\n";
body += "  let act = null;\n";
body += "  if (planned !== undefined && planned !== null && planned !== '' && planned > 0) {\n";
body += "    act = (worked / planned) * 100;\n";
body += "    actTotal += act; actCount++;\n";
body += "  }\n";
body += "  rows.push([dd, planned ?? '—', worked, req.toFixed(1) + '%', act !== null ? act.toFixed(1) + '%' : '—']);\n";
body += "}\n";
body += "dv.table(['Day','Planned','Worked','Required Eff.','Actual Eff.'], rows);\n";
body += "if (reqCount > 0) {\n";
body += "  dv.paragraph(`**Month avg — Required: ${(reqTotal/reqCount).toFixed(1)}%${actCount ? ' · Actual: ' + (actTotal/actCount).toFixed(1) + '%' : ''}** (based on ${reqCount} logged days)`);\n";
body += "} else {\n";
body += "  dv.paragraph('No days logged yet.');\n";
body += "}\n";
body += "```\n";

tR += front + body;

await tp.file.move("/Efficiency/Time Efficiency/" + input);
-%>
