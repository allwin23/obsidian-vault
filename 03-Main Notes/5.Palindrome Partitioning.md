---
tags:
  - coding-questions
  - dsa
status:
---
## 📋 Info

| Field               | Value                                                                                     |
| ------------------- | ----------------------------------------------------------------------------------------- |
| **Question Number** | 5                                                                                         |
| **Platform**        | LeetCode                                                                                  |
| **Difficulty**      | 🟡 Medium                                                                                 |
| **Topic**           | [[Recursion]]                                                                             |
| **Problem Link**    | [Link]([Palindrome Partitioning](https://leetcode.com/problems/palindrome-partitioning/)) |
| **Date Solved**     | 2026-01-03                                                                                |
| Intuition Log       |                                                                                           |


---

## ❓ Question:

> Given a string `s`, partition `s` such that every substring of the partition is a **palindrome**. Return _all possible palindrome partitioning of_ `s`.

**Example 1:**

**Input:** s = "aab"
**Output:** [["a","a","b"],["aa","b"]]

**Example 2:**

**Input:** s = "a"
**Output:** [["a"]]



## **Constraints:**

---

## 💻 Solution :


```cpp
class Solution {

public:

    vector<vector<string>> result;

    vector<string> buff;

  

    void partition(string& s, int index) {

        if (index == s.size()) {

            result.push_back(buff);

            return;

        }

  

        for (int i = index; i < s.size(); i++) {

            if (isPalindrome(s, index, i)) {

                buff.push_back(s.substr(index, i - index + 1));

                partition(s, i + 1);

                buff.pop_back();

            }

        }

    }

  

    bool isPalindrome(const string& s, int left, int right) {

        while (left < right) {

            if (s[left] != s[right]) return false;

            left++;

            right--;

        }

        return true;

    }

  

    vector<vector<string>> partition(string s) {

        partition(s, 0);

        return result;

    }

};
```



---


## ⏱️ Complexity :

| Type      | Complexity |
| --------- | ---------- |
| **Time**  | O(n)       |
| **Space** | O(1)       |
****
---

## 💡 Key Insight :
Same for loop template the Substring  conditioning is little bit different and because the i - index +1 should be added at all cost but I fucked up a simple palindrome logic that why i started from centre i don't know but the thing is very simple and others  stuffs has to be made left at start and right at end while left < right thing has to be called off 


---

## 🔗 Related Questions

|Question|Topic|Difficulty|
|---|---|---|
|[[]]|||
|[[]]|||
