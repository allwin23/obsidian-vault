---
tags:
  - coding-questions
  - dsa
status:
---
	## 📋 Info

| Field               | Value                                                              |
| ------------------- | ------------------------------------------------------------------ |
| **Question Number** | 3                                                                  |
| **Platform**        | LeetCodet                                                          |
| **Difficulty**      | 🟡 Medium                                                          |
| **Topic**           | [[Recursion]]                                                      |
| **Problem Link**    | [Link](https://leetcode.com/problems/combination-sum/description/) |
| **Date Solved**     | 2025-12-19                                                         |
| Intuition Log       |                                                                    |


---

## ❓ Question:

> Given an array of **distinct** integers `candidates` and a target integer `target`, return _a list of all **unique combinations** of_ `candidates` _where the chosen numbers sum to_ `target`_._ You may return the combinations in **any order**.

The **same** number may be chosen from `candidates` an **unlimited number of times**. Two combinations are unique if the frequency of at least one of the chosen numbers is different.

The test cases are generated such that the number of unique combinations that sum up to `target` is less than `150` combinations for the given input.



## **Constraints:**

---

## 💻 Solution :


```cpp
class Solution {
public:
    vector<vector<int>> combinationSum(vector<int>& candidates, int target) {
        vector<vector<int>> result;
        vector<int> buff;
        solve(candidates, target, 0, buff, 0, result);
        return result;
    }
    
private:
    void solve(vector<int>& nums, int target, int index, vector<int>& buff, int sum, vector<vector<int>>& result) {
        
        // Base case 1: Found valid combination
        if(sum == target) {
            result.push_back(buff);
            return;
        }
        
        // Base case 2: Exceeded target OR no more elements
        if(sum > target || index >= nums.size()) {
            return;
        }
        
        // Choice 1: Include nums[index] (can use again, so stay at same index)
        buff.push_back(nums[index]);
        solve(nums, target, index, buff, sum + nums[index], result);  // index stays same!
        buff.pop_back();
        
        // Choice 2: Skip nums[index], move to next
        solve(nums, target, index + 1, buff, sum, result);
    }
};
```
---


## ⏱️ Complexity :

| Type      | Complexity            |
| --------- | --------------------- |
| **Time**  | O(2^(target/min) * k) |
| **Space** | O(target/min)         |

---

## 💡 Key Insight :

this question i cracked it expectI missed few iteration things they are something like base condition of index reaching the final element 
i need to use the base condition here which i forgot except the logic of next elment with incule and exclude was really nice this question make the understanding of the appraoch evern beter 

---

## 🔗 Related Questions

|Question|Topic|Difficulty|
|---|---|---|
|[[]]|||
|[[]]|||
